{{define "title"}}Admin — AITU Connect{{end}}

{{define "content"}}
<div class="d-flex justify-content-between align-items-center mb-4">
  <div>
    <h1 class="fw-bold mb-0">Admin Panel</h1>
    <p class="text-muted mb-0">Manage campus content (posts, canteen news)</p>
  </div>
</div>

<div class="row g-4">
  <div class="col-md-6">
    <div class="card shadow-sm">
      <div class="card-body">
        <h5 class="fw-bold">Quick actions</h5>
        <ul class="mb-0 text-muted">
          <li>Create canteen updates</li>
          <li>Edit / delete news by ID</li>
          <li>Create official posts</li>
        </ul>
      </div>
    </div>
  </div>
</div>

{{end}}

{{template "base" .}}
